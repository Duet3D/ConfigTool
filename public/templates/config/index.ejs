; Configuration file for RepRapFirmware on <%- model.boardType %>
; executed by the firmware on start-up
;
; generated by RepRapFirmware Configuration Tool v<%- version %> on <%- (new Date()).toString() %>

<%- sections({
    "General": await render("config/general"),
    "Accessories": await render("config/accessories"),
    "Network": await render("config/network")
}) %>
<%  if (model.boards.some(board => board.canAddress)) { -%>

; Wait a moment for the CAN expansion boards to become available
G4 S2
<%  } -%>

<%- sections({
    "!Drivers": await render("config/drivers/index"),
    "Kinematics": await render("config/kinematics"),
    "Axes": await render("config/axes"),
    "Extruders": await render("config/extruders"),
    "Probes": await render("config/probes"),
    "Endstops": await render("config/endstops"),
    "!Compensation": await render("config/compensation/index"),
    "Sensors": await render("config/sensors"),
    "Heaters": await render("config/heaters"),
    "Spindles": await render("config/spindles"),
    "Lasers": await render("config/lasers"),
    "Fans": await render("config/fans"),
    "Tools": await render("config/tools"),
    "Miscellaneous": await render("config/miscellaneous")
}) %>
