<%  if (model.move.idle.timeout > 0) { -%>
M906 I<%- precise(model.move.idle.factor * 100) %> T<%- model.move.idle.timeout %> ; set motor current idle factor and idle timeout
<%  } else if (preview) { -%>
; NOTE: This section is not used in your configuration
<%  } -%>