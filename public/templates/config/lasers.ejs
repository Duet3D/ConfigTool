<%  const laserPort = getPort(ConfigPortFunction.laser, 0);
    if (laserPort !== null) { -%>
M452 <%- params({
    C: laserPort.toString(),
    F: laserPort.frequency,
    R: model.configTool.laser.maxIntensity,
    S: model.configTool.laser.sParamSticky ? 1 : 0
}) %> ; configure Laser port
<%  } -%>
