<%  if (!preview) { -%>
; tpost<%- toolNumber %>.g
; called after tool <%- toolNumber %> has been selected
;
; generated by RepRapFirmware Configuration Tool v<%- version %> on <%- (new Date()).toString() %>

<%	}
    if (model.configTool.waitForToolTemperatures) { -%>
M116 P<%- toolNumber %> ; wait for tool temperatures to be reached
<%	} else if (preview) { -%>
; No content in this file
<%  } -%>
