<%  if (!preview) { -%>
; <%- filename %>
; called to deploy a physical Z probe
;
; generated by RepRapFirmware Configuration Tool v<%- version %> on <%- (new Date()).toString() %>

<%	}

    if (probe.type == ProbeType.blTouch) { -%>
M280 P<%- getProbeServoIndex(probeIndex) %> S10	; deploy BLTouch
<%	} else { -%>
; insert codes for deploying the Z probe here
<%	} -%>
