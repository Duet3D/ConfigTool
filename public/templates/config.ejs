; Configuration file for RepRapFirmware on <%- model.boardType %>
; executed by the firmware on start-up
;
; generated by RepRapFirmware Configuration Tool v<%- version %> on <%- (new Date()).toString() %>
<%  if (model.boards.some(board => board.canAddress)) { -%>

; Wait a moment for the CAN expansion boards to become available
G4 S2
<%  } -%>

; General
<%- await render("config/general.ejs") -%>

<%- await render("config/drivers/index.ejs") -%>

; Kinematics
<%- await render("config/kinematics.ejs") -%>

; Axes
<%- await render("config/axes.ejs") -%>

; Axes
<%- await render("config/probes.ejs") -%>

; End of config.g
<%- await render("config/end.ejs") -%>
