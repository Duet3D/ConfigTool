; retractprobe.g
; called to retract a physical Z probe
;
; generated by RepRapFirmware Configuration Tool v2 on <%- (new Date()).toString() %>
<%	if (template.probe.type == "bltouch") { -%>
M280 P<%- template.probe.pwm_channel %> S90<%- template.probe.pwm_inverted ? " I1" : "" %>
<%	} else { -%>
; insert codes for retracting the Z probe here
<%	} -%>
