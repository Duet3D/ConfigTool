; retractprobe.g
; called to retract a physical Z probe
;
; generated by RepRapFirmware Configuration Tool v<%- version %> on <%- (new Date()).toString() %>
<%	if (template.probe.type === 'bltouch') {
		if (template.firmware < 3) { -%>
M280 P<%- template.probe.pwm_channel %> S90<%- template.probe.pwm_inverted ? ' I1' : '' %> ; retract BLTouch
<%		} else { -%>
M280 P0 S90 ; retract BLTouch
<%		}
	} else { -%>
; insert codes for retracting the Z probe here
<%	} -%>
