; run-once.g
; called after config.g. When it has been executed, it is automatically deleted!
;
; generated by RepRapFirmware Configuration Tool v<%- version %> on <%- (new Date()).toString() %>
M552 S0 ; disable network
G4 P500 ; wait half a second
M552 S1 ; enable network
G4 P1000 ; wait a second
M587 S"<%- template.network.ssid %>" P"<%- template.network.ssid_password %>"<%- template.network.dhcp ? '' : ` I${template.network.ip} J${template.network.netmask} K${template.network.gateway}` %> ; configure WiFi
M552 S1 ; enable network
